# datasets/

**Phase:** Phase 2
**Owner:** Haridharani
**Status:** Pipeline code complete (see `simulation/`); no data generated
yet — requires running `simulation/scripts/generate_dataset.py` on a GPU
workstation with CARLA + Sionna RT installed. See `metadata/CHANGELOG.md`.

## Purpose
Synchronized multimodal dataset (CARLA + Sionna RT) per 09_Dataset_Design_and_Annotation_Guide. See configs/dataset.yaml for schema and paths.

This folder is part of the fixed Aegis-V2X repository structure
(Master Project Instructions). Do not add files here outside of the
phase listed above without updating docs/module_decomposition.md.
